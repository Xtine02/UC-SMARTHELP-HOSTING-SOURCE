services:
  - type: web
    name: uc-smarthelp
    env: node
    buildCommand: cd UC-SmartHelp-main && npm install && npm run build
    startCommand: cd UC-SmartHelp-main && npm start
    staticPublishPath: ./UC-SmartHelp-main/dist
