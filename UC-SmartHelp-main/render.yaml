services:
  - type: web
    name: uc-smarthelp
    buildCommand: rm -rf node_modules package-lock.json && npm cache clean --force && npm install --production=false && npm run build
    startCommand: npm run server
    envVars:
      - key: NODE_ENV
        value: production
