services:
  - type: web
    name: uc-smarthelp-frontend
    buildCommand: bash build.sh
    startCommand: cd UC-SmartHelp-main && npm start
    envVars:
      - key: NODE_ENV
        value: production
